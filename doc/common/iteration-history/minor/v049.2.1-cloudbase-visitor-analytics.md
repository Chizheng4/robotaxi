# v049.2.1 CloudBase 访问记录接入

## 价值目标

保持 GitHub Pages 公开地址和免费静态发布不变，使用腾讯云 CloudBase 免费环境保存少量匿名登录记录，让作品访问情况可以长期查看，同时不采集用户行为或业务数据。

## 完成任务

- [x] 接入 CloudBase Web SDK，并配置环境 `robotaxi-visit-records-d8e34876e`。
- [x] 访问记录只在“金星”成功进入平台后创建；“访问”管理入口不计入普通访问。
- [x] 建立 `visitorAnalytics` 云函数，统一处理开始、心跳、结束、密码验证和周期查询。
- [x] 固定最小数据合同，不采集原始 IP、精确位置、页面路径、点击和业务数据。
- [x] 保留桌面和手机共用的访问记录界面，CloudBase 异常不阻塞平台登录。
- [x] 同步字段字典、配置文档、专项验证与提交前门禁。
- [ ] 在 CloudBase 控制台创建集合、部署云函数、设置安全域名和密钥。
- [ ] 发布 GitHub Pages 后完成真实访客写入、时长更新和管理查询验收。

## 验收边界

本版本完成仓库内可提交、可部署的工程闭环。CloudBase 控制台资源和公网写入属于外部环境验收，在用户完成控制台配置与正式发布后继续验证，不将尚未发生的公网结果表述为已完成。
