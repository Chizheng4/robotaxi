# v049.13.22 EdgeOne 源包门禁续修

## 根因

- v049.13.21 已正确生成 `dist` 源包和 `dist/.edgeone` 预构建产物。
- 固定 publish 错误要求源包存在仅属于预构建产物的 `index.js` 与 `config.json`，因此在 deploy 前停止。

## 修复

- 源包检查 `auth.js`、`qualify.js`、`records.js` 与共享 `visitAnalytics.js`。
- 预构建继续检查编译入口、配置和三条访问路由。
- dry 专项直接验证两层实际文件，不访问网络、不读取环境变量、不触发部署。

## 非目标

- 不修改 v049.13.21 的业务、性能、认证、页面或公网诊断合同。
- 不改写 v049.13.21 commit/tag，不手工绕过固定 publish。

## 验收

- [x] 源包与预构建双层 dry 门禁。
- [x] 完整提交门禁。
- [ ] 固定 publish 单次正式发布与公网动态验收。
