# v042.0.11 README 结构图渲染

## 目标

让站内项目 README 直接显示清晰结构图，并从发布流程保证 README 更新自动同步到网站。

## 完成内容

- README 服务解析 `flowchart LR/TB` 的节点、中文多行标签、方向、普通连线、双向连线和虚线关系。
- 结构图直接渲染为轻量 SVG，不再折叠、不展示 Mermaid 源码，也不引入大型运行时依赖。
- 桌面按浮层宽度展示，手机保持可读字号并在单张图内部横向浏览。
- GitHub Pages 发布合同升级为生产 README 与仓库 README 内容完全一致，文档更新无需维护前端副本。
- 浏览器合同验证全部 README 结构图均生成实际节点且源码不外露。

## 验证

- `bash scripts/check-before-commit.sh` 通过。
- README 的 10 张结构图在桌面和 `390 × 844` 手机真实浏览器中直接渲染。
- GitHub Pages README 同步合同和现有地图、详情、列表交互回归通过。
