# v049.13.18 完整发布与访问入口恢复

## 状态

已完成。

## 根因

固定发布指令只部署静态 `dist`，每次运行都会覆盖已通过临时完整包部署的 Edge Functions。访问认证请求因此收到 SPA HTML；前端没有拒绝非 JSON 的成功响应，密码弹框被关闭但无法进入访问概览，形成“点击无响应”。

## 范围

- 固化跨 Task 产品草案隔离与 Engineering 串行交付规则。
- 让正式构建确定性生成静态资源、Edge Functions 和路由配置完整产物。
- 让固定 publish 指令只部署完整产物，并在公网验证静态版本和动态接口。
- 让访问密码按钮与 Enter 共享表单提交合同，完整显示加载、错误和成功状态。

## 非目标

- 不修改访问统计业务合同、KV 字段、业务对象或字段字典。
- 不读取或输出 Secret，不制造自动 QA 访问记录。
- 不处理其他 backlog。

## 验收

- [x] 按钮与 Enter 均可提交；失败在弹框内反馈，成功才进入访问概览。
- [x] 桌面与 390 手机无横向溢出。
- [x] 完整部署产物包含三条访问函数路由，固定 publish 不再部署纯静态 `dist`。
- [x] 发布脚本已建立公网 manifest、QA qualify `AUTOMATED_QA` 与 records JSON 401 强制门禁。
- [x] 已完成版本归档和发布前工程验收；commit、tag、push、EdgeOne deployment 与正式域名结果由固定 publish 串行确认。
