# v043.0.5 GitHub Pages 环境保护适配计划

状态：已完成

## 目标

让版本发布符合 github-pages 环境只允许 main 部署的保护规则，并保留可靠的版本标签校验。

## 任务

- [x] 恢复 main 触发 Pages 部署；
- [x] Actions 校验前显式同步远端标签；
- [x] 校验 VERSION、Tag 与 HEAD 一致；
- [x] 调整 publish 为先 Tag 后 main；
- [x] 更新发布合同、版本记录和站内更新记录。

## 验收

- 部署来源满足 github-pages 的 main 环境保护规则；
- Commit Message 不参与发布成功判定；
- main 触发前远端已经存在对应 Tag；
- 发布合同和完整提交前检查通过。

## 验证结果

- workflow 与 publish 顺序合同通过；
- Tag 显式同步和 Tag/HEAD 校验已纳入 Actions；
- 业务代码、页面功能和模拟运行均未修改。
