# 当前大版本迭代

当前大版本：v041 供给端能力建设。

当前状态：执行中。v041 从供给端业务底座开始，先收敛 DemandProfile 统一对象，再逐步进入需求预测、供应计划、供给单、运营准入和 Robotaxi 资产进入运营的闭环。

最近完成大版本：v040 Robotaxi 运维状态治理与任务优先级调度。

## v041.1 需求画像统一对象落地

目标：

1. 将供应管理下的需求画像收敛为统一 `DemandProfile` 对象；
2. 使用 `target_object_type / target_object_id` 表达画像作用对象；
3. 废弃 `profile_type` 作为主口径，仅保留旧数据兼容；
4. 保持旧快照和现有页面可加载；
5. 不把需求预测、供应计划和供给单提前接入模拟运行主路径。

执行内容：

- 初始化模块生成统一 `demandProfiles`；
- 运行态恢复支持旧三类画像迁移；
- 前端需求画像页面展示统一目标对象字段；
- 前端需求画像页面前置展示关键配置数字和计算数字；
- 前端需求画像页面支持配置功能，按 Place、ServiceArea、Zone 类型展示不同配置字段；
- Zone 画像按包含的 Place / ServiceArea 画像汇总需求与服务容量；
- 字段字典同步统一字段和中文展示；
- 保留旧三类画像拆分作为隐藏兼容数据。
