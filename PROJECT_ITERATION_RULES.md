# Robotaxi 项目迭代规则迁移说明

本文件已不再作为迭代规则源。

当前唯一规则入口为：

`doc/iteration-rules.md`

细分规则集中维护在：

`doc/rules/`

保留本文件仅用于兼容旧版本记录和根目录提示，避免后续 Codex 或人工协作者误以为项目规则散落在多个位置。

后续新增或修改迭代规则时，请更新 `doc/iteration-rules.md` 或 `doc/rules/` 下的对应规则文件，不要在本文件继续复制规则内容。
